# include<stdio.h>

int main()
{
    /*
    Constants( const ): Values that don't change and  are fixed.
     There are 3 types of constants
     Integer Constants ( 1, 2, 3, 0, -1, -2, -1 )
     Real Constants ( 1.0, 3.14, -2.4 )
     Character Constants ( 'a', 'A', '*', '#', '&' ) 
    */
    return 0;
}