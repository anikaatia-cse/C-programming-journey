// 1. HEADER SECTION (Pre-processor Directives)
#include <stdio.h> // Standard Input Output Library
// 2. MAIN FUNCTION (The Entry Point)
int main()
{
    // 3. VARIABLE DECLARATION (Creating boxes for data)
    int gpa_score = 4;
    // 4. ACTION STATEMENTS (Doing the actual work)
    printf("Welcome to CSE! Your starting target is a %d.0 GPA\n", gpa_score);
    // 5. RETURN STATEMENT (Telling the system, we're done!)
    return 0;
}